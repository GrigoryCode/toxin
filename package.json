{
  "name": "toxin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.pug --open",
    "build": "rm -R docs && parcel build src/index.pug -d docs --public-url ./",
    "clear": "rm -R docs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "pug": "^2.0.3",
    "sass": "^1.19.0"
  },
  "dependencies": {
    "jquery": "^3.4.0",
    "normalize.css": "^8.0.1"
  }
}
